import equationresolver

def separate(equ):
    splitted_equ = string(equ.split(','))
    if(string(splitted_equ[0].contains('x^2'))):  # 2° grado
        n1 = splitted_equ[0]  # x^2
        pos = n1.find('x^2')
        a = int(n1[:pos])
    else:                        # 1° grado
    
    length = len(splitted_x2)

    if (string(splitted_x2[0].contains('x^2'))):
        n = splitted_x2[0]
        pos = n.find('x^2')
        a = int(n[:pos])
        if(pos != -1)
    return a,b,c