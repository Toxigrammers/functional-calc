def rationalFunction(x1, x2):
    # inserire funzioni controllo impossibile
    Dominio(x2)
def dominio(x2):
    splitted_x2 = string(x2.split(','))
    if((x2.find('x^2')) == -1):
        
    else:
        # 2° grado
    length = len(splitted_x2)

    if (string(splitted_x2[0].contains('x^2'))):
        n = splitted_x2[0]
        pos = n.find('x^2')
        a = int(n[:pos])
        if(pos != -1)
    